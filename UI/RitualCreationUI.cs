using Godot;
using System;

/*
 * Es probable que haya que poner esto como una escena aparte
 * y encontrar una forma de pasarle la información de si el mouse
 * está dentro de la UI a la cámara a partir de aquí
 * puede que tenga que ser con get node
 *
*/
public partial class RitualCreationUI : CanvasLayer
{
	// Called when the node enters the scene tree for the first time.
	public override void _Ready()
	{
	}

    // Called every frame. 'delta' is the elapsed time since the previous frame.
    public override void _Process(double delta)
	{
	}
}
